{
  "name": "sic-portal-competencia",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp *.html dist/ 2>/dev/null && cp -r img dist/ 2>/dev/null || true",
    "start": "node server.js",
    "lint": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
